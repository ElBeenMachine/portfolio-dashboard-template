# Prepare the version tag for the job
prepare:
    needs:
        - job: eslint
    stage: prepare
    before_script: []
    script:
        - export TAG=$(node -p "require('./package.json').version")
        - echo "TAG=$TAG" >> variables.env
    services: []
    artifacts:
        reports:
            dotenv: variables.env
