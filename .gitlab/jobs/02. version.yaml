update_version:
    needs:
        - job: eslint
    stage: version
    before_script: []
    script:
        - echo $CI_MERGE_REQUEST_LABELS
    only:
        - merge_requests
        - staging

get_version:
    stage: prepare
    before_script: []
    script:
        - echo $(npm version)
    only:
        - merge_requests
        - production
