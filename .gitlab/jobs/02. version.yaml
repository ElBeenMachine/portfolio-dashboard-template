update_version:
    needs:
        - job: lint
    stage: version
    script:
        - echo $CI_MERGE_REQUEST_LABELS
    only:
        - merge_requests
        - staging

get_version:
    stage: prepare
    script:
        - echo $(npm version)
