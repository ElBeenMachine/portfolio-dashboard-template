# Build the project for AMD64 hardware
build_amd64:
    needs:
        - job: eslint
    stage: build
    script:
        - mkdir -p builds
        - docker build . -t $PROJECT_NAME:$CI_COMMIT_SHA
        - docker save $PROJECT_NAME:$CI_COMMIT_REF > builds/$PROJECT_NAME:$CI_COMMIT_REF-amd64.tar
    artifacts:
        paths:
            - builds

# Build the project for ARM64 hardware
build_arm64:
    needs:
        - job: eslint
    stage: build
    script:
        - mkdir -p builds
        - docker build . -t $PROJECT_NAME:$CI_COMMIT_SHA
        - docker save $PROJECT_NAME:$CI_COMMIT_REF > builds/$PROJECT_NAME:$CI_COMMIT_REF-arm64.tar
    artifacts:
        paths:
            - builds
    tags:
        - arm_kubernetes
